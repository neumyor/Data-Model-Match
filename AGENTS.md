# Agent 开发规范

本文档是 `DataModelMatch` 项目面向所有 Agent 的开发规范。所有 Agent 在开始实现前都必须阅读并遵守本文档；如果子目录存在更具体的 `AGENTS.md`，以更具体的规范为准，但不得削弱本文档中的安全、验收和只读要求。

## 1. 基本工作方式

- 先理解现有代码、目录结构、运行方式、测试方式和数据模型，再开始修改。
- 开始实现前必须先明确目标、范围、依赖、接口、失败场景和验收标准。
- 优先复用项目已有的模式、库和抽象；没有充分理由时，不新增重复的基础设施。
- 修改应保持范围收敛，不顺手重构与当前任务无关的代码。
- 代码应简洁、清晰、可维护。命名要准确，控制流要直接，避免不必要的抽象、隐式行为和重复逻辑。
- 完成实现后必须进行针对性验证，并向主 Agent 汇报修改内容、验证结果、已知限制和未完成事项。

## 2. 大型系统的多 Agent 协作

当任务属于大型系统开发，或可以拆分为多个相对独立的模块、能力或验证面时，必须采用多 Agent 并行开发。

### 2.1 拆分与分工

- 主 Agent 负责总体架构、任务拆分、接口决策、依赖协调、合并、集成测试和最终验收。
- 子 Agent 负责边界清晰的子任务，例如独立模块、适配器、测试、文档、调查或原型验证。
- 子任务必须在开始前写清楚：
  - 目标和非目标；
  - 输入、输出和依赖；
  - 不得修改的文件或目录；
  - 预期接口；
  - 验证方法和完成标准。
- 有共享接口或共享数据模型时，先由主 Agent 设计并确认接口，再并行实现依赖该接口的工作。
- 子 Agent 不得擅自改变公共接口、整体架构或其他 Agent 的职责范围。发现需要改变时，先向主 Agent 提出方案和影响。

### 2.2 模型分配

- 子 Agent 使用的模型按任务难度分配：
  - 复杂架构、跨模块设计、并发与状态管理、关键集成：优先使用 `Terra Medium`；
  - 中等复杂度的功能实现、适配和测试：使用 `Luna Medium` 或 `Terra Medium`；
  - 简单、边界明确的机械性任务：最低也使用 `Luna Medium`。
- 子 Agent 的模型等级最高为 `Terra Medium`，最低为 `Luna Medium`。不得使用低于 `Luna Medium` 或高于 `Terra Medium` 的模型执行本项目的子任务。
- 主 Agent 可以根据风险和依赖关系重新分配任务，但不能以并行开发替代主 Agent 的集成和验收责任。

### 2.3 主 Agent 验收与 wiring

- 所有子任务完成后，主 Agent 必须逐项检查实现是否符合原任务、接口约定和项目现有规范。
- 主 Agent 必须负责 wiring，包括依赖注入、模块注册、路由或命令接入、配置接入、错误处理和生命周期连接。
- 不能仅根据子 Agent 的自述判定完成；至少要阅读关键 diff，并运行与改动相关的测试或验证命令。
- 集成失败、接口不一致或测试缺失时，主 Agent 必须修正、退回或补齐，不能把未验证的实现直接视为完成。
- 最终验收至少应覆盖：
  - 主流程；
  - 主要失败路径；
  - 接口输入输出；
  - 关键依赖和配置；
  - 相关单元测试、集成测试或端到端测试。

## 3. Agent-powered 系统的真实 LLM 测试

如果构建的系统包含 Agent、模型编排、工具调用、规划、提示词执行或其他依赖 LLM 行为的能力，必须使用真实 LLM API 进行测试。仅使用 mock、静态 fixture 或本地假模型不能作为唯一验收依据。

### 3.1 默认模型与 endpoint

- 默认模型：`glm-5.3-flash`
- 默认 endpoint：`https://llm-center.modelbest.co/v1/chat/completions`
- 默认 LLM 配置文件：项目根目录的 `config.llm.json`
- 所有 Agent 和测试实现必须优先从 `config.llm.json` 读取 `endpoint`、`apiKey`、`model` 和 `stream` 配置，不得要求用户通过环境变量提供这些配置。
- 如果需要支持其他配置来源，必须把 `config.llm.json` 作为默认且优先级最高的来源，并在 plan 中说明覆盖规则；不得静默覆盖文件中的 endpoint、模型或 API Key。
- API 使用 OpenAI Chat Completions 兼容格式：
  - `Content-Type: application/json`
  - `Authorization: Bearer <config.llm.json 中的 apiKey>`
  - 请求体至少包含 `model`、`messages` 和 `stream: false`

配置文件格式：

```json
{
  "endpoint": "https://llm-center.modelbest.co/v1/chat/completions",
  "apiKey": "<本地 API Key>",
  "model": "glm-5.3-flash",
  "stream": false
}
```

使用配置文件发起请求的示例：

```sh
LLM_ENDPOINT="$(jq -r '.endpoint' config.llm.json)"
LLM_API_KEY="$(jq -r '.apiKey' config.llm.json)"
LLM_MODEL="$(jq -r '.model' config.llm.json)"
LLM_STREAM="$(jq -r '.stream' config.llm.json)"

curl -X POST "$LLM_ENDPOINT" \
  -H 'Content-Type: application/json' \
  -H "Authorization: Bearer $LLM_API_KEY" \
  -d "{\"model\":\"$LLM_MODEL\",\"messages\":[{\"role\":\"user\",\"content\":\"你好\"}],\"stream\":$LLM_STREAM}"
```

### 3.2 凭据和测试要求

- API Key 存放在根目录 `config.llm.json` 的 `apiKey` 字段中。该文件已由 `.gitignore` 忽略，禁止取消忽略或提交。
- 禁止把 API Key 复制到源码、测试文件、文档、日志、快照、提交记录或构建产物中。
- Agent 开始真实 LLM 测试前必须检查 `config.llm.json` 存在、JSON 格式正确、`endpoint`/`apiKey`/`model` 字段非空，并在测试结束后确认配置文件仍处于 Git ignored 状态。
- 测试输出中不得打印完整 API Key、Authorization header 或包含敏感信息的完整请求。
- 真实 API 测试应覆盖至少一个成功请求，以及系统实际关心的错误、超时、空响应、格式异常或工具调用场景。
- 真实 API 测试必须可重复运行，并明确依赖根目录 `config.llm.json`；没有该文件或凭据无效时应明确失败或按项目既有约定跳过，但不能伪装成真实测试已经通过。
- 对模型生成内容的断言应优先验证结构、协议、工具调用和关键行为，不应依赖完全固定的自然语言措辞。
- 真实 API 测试可能产生费用、延迟和外部副作用。测试应控制请求数量，避免发送真实用户隐私和不可逆操作。

## 4. 接口设计与实现流程

接口设计是实现前的重点工作，不能边写实现边临时决定公共接口。

### 4.1 实现前的 plan

实现前必须先形成简短 plan，至少回答：

- 调用方和被调用方分别是谁；
- 接口解决的具体问题和不负责的问题；
- 请求、响应、事件或消息的字段及其含义；
- 必填字段、可选字段、默认值和兼容策略；
- 同步、异步、流式、重试、超时和取消语义；
- 错误类型、错误码、错误信息和调用方处理方式；
- 幂等性、并发安全、状态持久化和资源释放要求；
- 认证、授权、敏感数据和日志脱敏要求；
- 如何测试正常路径、边界条件和失败路径。

### 4.2 接口设计原则

- 公共接口应表达稳定的业务意图，不暴露不必要的内部实现细节。
- 输入和输出必须有明确类型；避免使用无约束的 `any`、任意键值对象或依赖隐含约定的字符串。
- 接口命名、参数顺序、返回值和错误方式应与项目现有风格保持一致。
- 默认行为必须明确记录；重要默认值不得藏在深层实现中导致调用方无法发现。
- 变更已有接口时，先评估所有调用方、序列化格式、配置、测试和兼容影响。
- 对跨模块、跨进程、网络、持久化、队列和模型边界进行显式校验；同进程且由类型系统保证的内部调用不重复堆叠无价值的运行时校验。
- 不为了“灵活”而过早设计过度通用的接口。先满足明确场景，并为后续扩展保留合理空间。
- 接口设计完成后，主 Agent 或负责 Agent 必须通过类型检查、契约测试、示例调用或小型原型进行校验，再进入完整实现。

### 4.3 实现后检查

- 检查接口文档、类型定义、实现、调用方和测试是否一致。
- 检查异常是否可诊断，是否会吞掉错误，是否会产生资源泄漏或重复副作用。
- 检查超时、取消、重试和并发行为是否符合接口承诺。
- 检查日志和错误信息是否泄露密钥、用户隐私、提示词或模型返回中的敏感数据。
- 运行最小相关验证，再根据改动范围扩展到集成测试、真实 LLM 测试或端到端测试。

## 5. `_reference_only` 参考代码

当实现需要使用或集成开源工具时，可以在 `_reference_only` 下 clone 对应的开源仓库，用于阅读其文档、类型定义、测试和实现方式。

- `_reference_only` 仅用于参考，不属于产品源码。
- `_reference_only` 下的任何已有仓库和文件均为只读，禁止修改、格式化、打补丁、生成文件或提交变更。
- 目前已有 `_reference_only/deepseek-harness`，可以参考其架构、Agent 规范、测试组织和相关工具用法。
- 参考代码必须遵守其许可证和归属要求；不得未经确认复制受许可限制的代码、文档或素材。
- 使用参考仓库前，先确认版本、许可证、适用 API 和项目实际依赖版本，不能只凭搜索到的片段推断用法。
- 从参考仓库借鉴方案时，应在本项目中重新实现并适配本项目的接口、错误处理、配置和测试，不得直接改动参考仓库来“验证”方案。
- 如果确实需要修改参考仓库进行实验，应在 `_reference_only` 之外使用临时副本，并确保实验结果不会写回 `_reference_only`。

## 6. 完成标准

一个任务只有在以下条件全部满足时才可报告完成：

- 代码实现已完成，且没有把未决设计问题隐藏在实现中；
- 公共接口经过 plan、校验并与调用方保持一致；
- 相关测试已运行并记录结果；
- Agent-powered 系统已完成真实 LLM API 测试；
- 多 Agent 任务已由主 Agent 完成验收和 wiring；
- 文档、配置、错误处理和必要的迁移已同步；
- 未修改 `_reference_only` 中的任何内容；
- 未将凭据、临时文件或调试输出提交到项目中；
- 已明确说明未覆盖的风险、环境限制和后续工作。

## 7. 汇报格式

Agent 完成任务后，向主 Agent 汇报：

1. 完成了什么，以及修改了哪些文件；
2. 公共接口或数据模型发生了什么变化；
3. 运行了哪些验证，以及结果是什么；
4. 是否执行了真实 LLM 测试，使用了哪些非敏感配置；
5. 已知限制、未覆盖场景和建议的后续工作。
