# P0.1 Job 运行时语义补充 执行报告

## 状态

**`ACCEPTED`。**

P0.1 已冻结 Profile/Match job 在共享路径接入前必须遵守的运行时语义。该 gate 冻结契约和其机器可验证规则，不宣称已经完成生产 worker、HTTP、SSE transport 或 UI wiring。

## 交付

- `docs/semantic-contracts/job-runtime-semantics-v1.json`
  - 完整 ErrorCode retryable 映射。
  - `attempt`/`maxAttempts` 的 v1 兼容定义。
  - 可复算的确定性指数退避和 jitter。
  - 取消与 publish 的唯一胜负规则。
  - 私有 worker lease/recovery、SSE retention/replay 和 crash reconciliation 规则。
- `docs/semantic-contracts/semantic-contract-rfc.md`
  - 新增 P0.1 Runtime Semantics Supplement，明确策略文件的权威位置和公共 API 不变的边界。
- `tests/semantic_contracts/test_job_runtime_semantics.py`
  - 验证 ErrorCode 覆盖、retry 映射、预算边界、确定性 delay、取消/lease/SSE/publish 协议。

## 验证

执行：

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src uv run --no-project --with pytest --with pyarrow pytest -q tests/semantic_contracts tests/test_semantic_store.py
25 passed, 401 subtests passed
```

并使用 bundled Python 验证 `job-runtime-semantics-v1.json` 可解析，且 `git diff --check` 通过。

## 冻结输入

| 工件 | SHA-256 |
| --- | --- |
| `job-runtime-semantics-v1.json` | `9c16628857b0e2c402da5fba31ab0146487315615b2881421ecd0090dbab5adc` |
| `semantic-contract-rfc.md` | `d5f9fb49fed01f6d0b10485e66e45114c5676037bffab8f01a8403ac911e72d5` |
| `contract-manifest.json` | `5a21d7dfafb8909307067b7f103937d5ecbbcbcd481d9cf355a868f4bcba4ed5` |
| `schema-catalog.json` | `69fdb215e067fb9feb06e4a8e296fc256f9d481206044a0722c5a2027c6c04e4` |

## 后续约束

1. 后续 Profile/Match job runner、VLM retry、worker、SSE adapter 和崩溃恢复实现必须使用该策略，不能重新定义 retryability、attempt、lease 或 replay 行为。
2. 共享 `ResourceStore`、HTTP 或 SSE wiring 开始前，主 Agent 必须将本报告、策略文件和更新后的 Phase 0 执行记录提交并签署为同一冻结基线。
3. 生产实现必须补齐策略驱动的状态机、lease/recovery、retention 和故障注入集成测试；这些属于后续实现/集成验收，不能由本 P0.1 的静态契约测试代替。
4. 未执行真实 LLM/VLM 调用。P0.1 不调用模型；Phase 3 与 Phase 4 的真实模型门禁仍然适用。
